<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.ksilisk.leetty</groupId>
    <artifactId>leetty-parent</artifactId>
    <version>${leetty.project.version}</version>
    <name>leetty</name>

    <packaging>pom</packaging>

    <properties>
        <leetty.project.version>1.7</leetty.project.version>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <spring.boot.version>3.2.1</spring.boot.version>
        <spring.cloud.version>2023.0.0</spring.cloud.version>
        <graphql.codegen.plugin.version>1.50</graphql.codegen.plugin.version>
        <mojo.build.helper.plugin.version>3.5.0</mojo.build.helper.plugin.version>
        <graphql.dgs.version>9.1.0</graphql.dgs.version>
        <dgs.codegen.shared.core.version>6.1.2</dgs.codegen.shared.core.version>
        <maven.surefire.plugin.version>3.2.2</maven.surefire.plugin.version>
        <telegram.bots.api.version>6.9.7.1</telegram.bots.api.version>
        <spring.statemachine.version>4.0.0</spring.statemachine.version>
        <spring.plugin.image.builder>docker.io/paketobuildpacks/builder-jammy-full:0.3.369</spring.plugin.image.builder>
        <spring.plugin.image.runImage>docker.io/paketobuildpacks/run-jammy-full:0.1.41</spring.plugin.image.runImage>
    </properties>

    <modules>
        <module>leetty-telegram-bot</module>
        <module>leetty-web-service</module>
        <module>leetty-auth-service</module>
        <module>leetty-common</module>
        <module>telegram-bot-spring-boot-starter</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.netflix.graphql.dgs</groupId>
                <artifactId>graphql-dgs-platform-dependencies</artifactId>
                <version>${graphql.dgs.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring.cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.statemachine</groupId>
                <artifactId>spring-statemachine-bom</artifactId>
                <version>${spring.statemachine.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>com.netflix.graphql.dgs.codegen</groupId>
                <artifactId>graphql-dgs-codegen-shared-core</artifactId>
                <version>${dgs.codegen.shared.core.version}</version>
            </dependency>
            <dependency>
                <groupId>org.telegram</groupId>
                <artifactId>telegrambots</artifactId>
                <version>${telegram.bots.api.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven.surefire.plugin.version}</version>
            </plugin>
        </plugins>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring.boot.version}</version>
                    <configuration>
                        <image>
                            <env>
                                <BP_JVM_VERSION>17</BP_JVM_VERSION>
                                <BP_JVM_TYPE>JDK</BP_JVM_TYPE>
                            </env>
                            <builder>${spring.plugin.image.builder}</builder>
                            <runImage>${spring.plugin.image.runImage}</runImage>
                        </image>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>io.github.deweyjose</groupId>
                    <artifactId>graphqlcodegen-maven-plugin</artifactId>
                    <version>${graphql.codegen.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>build-helper-maven-plugin</artifactId>
                    <version>${mojo.build.helper.plugin.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>